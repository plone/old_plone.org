-c constraints.txt
-e src/ploneorg.core[test]
pas.plugins.authomatic == 1.0b2

# WSGI: A system for configuration of WSGI web components in declarative .ini format.
Paste

# we want the whole package
Plone